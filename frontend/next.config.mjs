/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  assetPrefix: '/Portfolio',
  basePath: '/Portfolio',
  images: {
    unoptimized: true,
  },
};

export default nextConfig; 